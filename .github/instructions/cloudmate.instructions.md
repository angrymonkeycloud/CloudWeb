---
applyTo: '**'
---

<!--
  Managed by CloudMate (AngryMonkey.CloudMate) - regenerated on every build.
  Do NOT edit this file directly, your changes will be overwritten.

  This file only defines CloudMate's default conventions. If this repository
  already has its own AI agent instructions (for example a root
  ".github/copilot-instructions.md", or another file under
  ".github/instructions/"), those project-specific instructions are more
  authoritative and take precedence over the defaults below whenever they
  conflict. See "Precedence" at the bottom of this file.
-->

# CloudMate Instructions

## C# Style

- Prefer explicit types over `var`. Use `var` only when the type is obvious from the right-hand side, the type name is long/generic, or usage is idiomatic and well known, such as Entity Framework Core queries (e.g. `var order = await context.Orders.FirstOrDefaultAsync(o => o.Id == id);`).
- Target the latest C# language features when they improve clarity:
  - Primary constructors.
  - Expression-bodied members for single-expression methods, properties, and constructors.
  - Null-conditional assignment (`?.` on the left-hand side of `=`).
  - Collection expressions (`[]`, `[.. other]`) instead of `new List<T> { ... }` or trailing `.ToList()` calls where applicable.
  - Pattern matching enhancements (`is` patterns, switch expressions, relational and logical patterns).
  - Target-typed `new()` when the declared type is already explicit on the left-hand side.
- Omit braces for single-statement `if`, `else`, `for`, `foreach`, and `while` bodies. Only add braces when the body contains more than one statement.
- Favor readability and consistency with the surrounding file over cleverness. Code must be clean, maintainable, and follow best practice.
- Name enums in the plural form (e.g. `ProjectSDKs`, `OrderStatuses`).

## Static Assets (CSS/JS/Images)

- Never author `.css` files directly, always author `.less` files instead.
- Source static assets (`.less`, `.js`/`.ts`, images) live under `src/`, in a subfolder matching their type (e.g. `src/css`, `src/js`, `src/images`), and are compiled or compressed into `wwwroot`.
- Razor isolated component styles (`Component.razor` + `Component.razor.less`) compile to a co-located `Component.razor.css` in the same folder as the component, using the existing Less-to-CSS compiler tooling. Never hand-author the isolated `.razor.css` file directly.
- A generic (non-isolated) `.less` file compiles into `wwwroot/css`. If the source `.less` file doesn't exist yet, create it under `src/css` first, then let the build compile it into `wwwroot/css`.
- Follow the Less naming convention described at https://github.com/angrymonkeydocs/css-naming-convention.

## Precedence

These are CloudMate's default conventions, applied as a baseline for every project. If this repository defines its own AI agent instructions elsewhere (for example a root `.github/copilot-instructions.md`, or another file under `.github/instructions/`), treat those project-specific instructions as more important and follow them whenever they conflict with the defaults above.
